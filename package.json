{
  "name": "bluetooth-puppeteer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "script": "ts-node index.ts",
    "build": "tsc",
    "docker-run": "docker run --platform linux/amd64 -i --init --cap-add=SYS_ADMIN --rm ghcr.io/puppeteer/puppeteer:latest node -e \"$(cat ./index.js)\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node18": "^18.2.2",
    "@types/node": "^20.6.2",
    "puppeteer": "^21.3.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
